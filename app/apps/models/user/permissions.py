from utils.permissions import FullModelPermissions

# Alias kept for backward compatibility — all viewsets that import
# HasUserPermissionUser continue to work without changes.
HasUserPermissionUser = FullModelPermissions
